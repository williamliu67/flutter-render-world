import 'package:flutter_render_world/label/view.dart';

typedef OnClickListener = void Function(View div);

typedef OnLongClickListener = void Function(View div);

typedef OnTouchEventListener = void Function(View div);